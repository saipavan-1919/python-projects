This atm transaction program is based on the csv file.
The csv file contains the data about the user like name, customer id, pin, balance.
  * It will ask the user to enter his name.
  * If user presants then only it will proceed further operation
  * It will ask user to enter his password for any transaction
  * if user gives correct data then only the operation gets performed.
